[build]
# PHYSICS: Instructs LLVM to enable all host-specific features (AVX-512, NEON, AES).
rustflags = ["-C", "target-cpu=native"]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
